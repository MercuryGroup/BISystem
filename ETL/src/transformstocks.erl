% Mercry group transform stock template.

-module(transformstocks).
-export([start/0, stop/0, init/0, transform/1, sendData/1, loop/1).

start() ->
	ok.

stop() ->
	ok.

init() ->
	ok.

transform([]) ->
	ok.

sendData([]) ->
	ok.

loop() ->
	ok.